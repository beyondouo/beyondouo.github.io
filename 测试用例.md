# Crypto Monitor 全功能测试用例

## 一、系统功能清单

| # | 模块 | 功能 |
|---|------|------|
| 1 | 行情监控 | 添加/删除自选币种（搜索框 + 热搜榜点击） |
| 2 | 行情监控 | 实时价格展示（WebSocket 推送 + 轮询降级） |
| 3 | 行情监控 | 多交易所自动探测（Binance/Gate.io/OKX/MEXC/Bitget/HTX） |
| 4 | 行情监控 | 手动指定优先交易所 |
| 5 | 行情监控 | 价格闪动动画（涨绿/跌红） |
| 6 | 行情监控 | 迷你走势图（Sparkline） |
| 7 | 行情监控 | 列表排序（价格/涨跌幅/24h最高/最低/成交额） |
| 8 | 行情监控 | 刷新间隔设置（5s/10s/30s/1min/5min） |
| 9 | 行情监控 | 法币切换（USD/CNY/EUR） |
| 10 | 市场概况 | 总市值、BTC占比、恐惧贪婪指数、24h变化 |
| 11 | 热搜榜 | 实时热搜/市值头部/24h涨幅榜（CoinGecko数据） |
| 12 | 热搜榜 | 点击添加/移除币种 |
| 13 | 搜索 | 币种搜索自动补全（本地 + CoinGecko API） |
| 14 | 搜索 | 键盘上下箭头选择 + Enter确认 |
| 15 | 价格预警 | 添加预警（币种/方向/目标价/冷却间隔） |
| 16 | 价格预警 | 删除预警 |
| 17 | 价格预警 | 预警触发：Toast提示 |
| 18 | 价格预警 | 预警触发：声音提示（普通/紧急） |
| 19 | 价格预警 | 预警触发：语音播报（中文TTS） |
| 20 | 价格预警 | 预警触发：浏览器通知 |
| 21 | 价格预警 | 预警触发：手机震动 |
| 22 | 价格预警 | 预警触发：屏幕跑马灯 |
| 23 | 价格预警 | 预警触发：屏幕爆闪 |
| 24 | 价格预警 | 预警导入/导出（JSON） |
| 25 | 价格预警 | 预警冷却时间（per-alert cooldown） |
| 26 | 强制预警 | 添加强制跌幅预警（币种/跌幅%/窗口/冷却） |
| 27 | 强制预警 | 删除/暂停/启用强制预警 |
| 28 | 强制预警 | 触发时：强制语音播报（无视语音开关） |
| 29 | 强制预警 | 触发时：紧急音效 + 跑马灯 + 爆闪 |
| 30 | 强制预警 | 实时跌幅监控面板（进度条） |
| 31 | 强制预警 | 导入/导出 |
| 32 | 通知渠道 | Server酱（微信推送）配置 + 测试 |
| 33 | 通知渠道 | PushPlus（微信推送）配置 + 测试 |
| 34 | 通知渠道 | Email（EmailJS）配置 + 测试 |
| 35 | 通知渠道 | iOS 锁屏通知（PWA + Service Worker） |
| 36 | 声音引擎 | Web Audio API 音效（普通/通知/紧急三种） |
| 37 | 声音引擎 | AudioContext 自动恢复（后台/锁屏/冻结） |
| 38 | 声音引擎 | HTML5 Audio 降级兜底 |
| 39 | 语音引擎 | 中文语音播报（Web Speech API） |
| 40 | 语音引擎 | 语音队列 + 防卡死 Watchdog |
| 41 | 语音引擎 | 引擎硬重置（连续失败3次） |
| 42 | 语音引擎 | 币种中文名称映射 / 逐字母拼读切换 |
| 43 | 持仓盈亏 | 添加持仓（币种/买入价/数量） |
| 44 | 持仓盈亏 | 删除持仓 |
| 45 | 持仓盈亏 | 实时盈亏计算（金额 + 百分比） |
| 46 | 持仓盈亏 | 行情列表内嵌盈亏标签 |
| 47 | 新闻聚合 | RSS多源聚合（CoinTelegraph/The Block/CryptoNews） |
| 48 | 新闻聚合 | 行情异动提醒（短时涨跌幅≥2%） |
| 49 | 新闻聚合 | 高交易额提醒 |
| 50 | 新闻聚合 | 筛选（全部/自选币种/重要） |
| 51 | 币种解锁 | 已知解锁事件列表 |
| 52 | 币种解锁 | 筛选（全部/自选/7天/30天） |
| 53 | 币种解锁 | 解锁进度条 + 价值估算 |
| 54 | AI分析 | 市场全景分析 |
| 55 | AI分析 | 自选币种诊断（RSI/量能/趋势） |
| 56 | AI分析 | 入手区间建议（支撑/阻力） |
| 57 | AI分析 | 风险评估（持仓+市场指标） |
| 58 | 主题 | 深色/浅色主题切换 |
| 59 | 主题 | 涨跌颜色反转 |
| 60 | 数据管理 | 全量数据导出（JSON） |
| 61 | 数据管理 | 数据导入（去重合并） |
| 62 | 快捷键 | / N A F M P H S T L E U I 等 |
| 63 | 币种卡片 | Hover 显示详情卡片（价格/简介） |
| 64 | 保活系统 | Edge 休眠标签恢复 |
| 65 | 保活系统 | WS 健康检查 + 自动重连 |
| 66 | 保活系统 | 计时漂移检测 |
| 67 | 保活系统 | Wake Lock 防冻结 |
| 68 | PWA | Service Worker 缓存 |
| 69 | PWA | iOS 添加到主屏幕 |
| 70 | 存储保护 | localStorage 配额保护 + 旧数据清理 |

---

## 二、黑盒测试用例（用户视角，不关心内部实现）

### 2.1 行情监控模块

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-001 | 添加新币种 | 在搜索框输入 `DOGE`，点击下拉结果或按 Enter | DOGE 出现在行情列表，显示价格、涨跌幅等 |
| BB-002 | 添加重复币种 | 先添加 BTC，再次在搜索框输入 BTC 添加 | Toast 提示"BTC 已在自选列表中"，不重复添加 |
| BB-003 | 删除币种 | 点击某币种行左侧 ✕ 按钮 | 币种从列表消失，数据停止更新 |
| BB-004 | 热搜榜添加 | 点击热搜榜中未添加的币种标签 | 标签显示 ✅ 已添加，币种出现在行情列表 |
| BB-005 | 热搜榜移除 | 点击已添加的币种标签（显示 ✕） | 币种从行情列表移除，标签恢复为可添加状态 |
| BB-006 | 价格自动刷新 | 添加 BTC 后等待 5 秒（默认间隔） | 价格数字更新，状态栏显示更新时间 |
| BB-007 | 价格闪动效果 | 观察价格变化时的数字颜色变化 | 上涨时短暂绿色闪烁，下跌时短暂红色闪烁 |
| BB-008 | 修改刷新间隔 | 下拉选择"30秒" | 状态栏显示"每30秒自动刷新"，实际更新变慢 |
| BB-009 | 切换法币 | 下拉选择 CNY | 价格前缀变为 ¥，数值按汇率换算 |
| BB-010 | 切换法币到 EUR | 下拉选择 EUR | 价格前缀变为 €，数值按汇率换算 |
| BB-011 | 排序-按价格升序 | 点击"最新价"表头 | 列表按价格从低到高排列，▲变▼ |
| BB-012 | 排序-按涨跌幅降序 | 点击"涨跌幅"表头 | 默认降序，涨幅最大的在最上面 |
| BB-013 | 排序-再次点击反转 | 再次点击"涨跌幅"表头 | 排序方向反转 |
| BB-014 | 空列表状态 | 删除所有币种 | 显示"暂无自选币种"空状态提示 |
| BB-015 | 搜索框自动补全 | 输入 `sol` | 下拉列表显示 SOL、Solana 等匹配结果 |
| BB-016 | 搜索框键盘操作 | 输入 `eth`，按 ↓ 选中，按 Enter | ETH 被添加到自选列表 |
| BB-017 | 搜索框 Esc 关闭 | 输入内容后按 Esc | 下拉列表关闭 |
| BB-018 | 币种 Hover 卡片 | 鼠标悬停在行情列表的币种名称上 | 弹出详情卡片，显示价格、涨跌幅、简介 |
| BB-019 | 迷你走势图 | 观察列表中的 Sparkline | 每个币种旁有迷你折线图，颜色与涨跌一致 |
| BB-020 | 切换交易所 | 手动选择"OKX" | 所有币种的交易所标签变为 OKX |

### 2.2 价格预警模块

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-021 | 添加价格预警-高于 | 打开预警面板，选 BTC，方向"高于"，输入目标价 100000，点添加 | 预警出现在列表中，Toast 提示已添加 |
| BB-022 | 添加价格预警-低于 | 选 ETH，方向"低于"，输入目标价 1000，点添加 | 预警出现在列表中 |
| BB-023 | 删除预警 | 点击预警条目右侧 ✕ | 预警从列表消失 |
| BB-024 | 预警触发-完整链路 | 设置 BTC 高于当前价（如当前 80000 设 85000），等价格到达 | ①Toast弹出 ②声音响 ③语音播报 ④浏览器通知 ⑤跑马灯横幅 ⑥爆闪 |
| BB-025 | 预警冷却 | 同一预警连续触发 | 在冷却期内不重复触发（Toast/声音/通知均不重复） |
| BB-026 | 首次触发 vs 后续触发 | 首次触发预警后，价格回落后再触发 | 首次用紧急音，后续用普通音 |
| BB-027 | 测试声音按钮 | 点击"🔊 测试" | 依次播放三种音效（普通/通知/紧急） |
| BB-028 | 测试语音按钮 | 点击"🗣️ 语音" | 语音播报"语音测试，比特币当前价格…" |
| BB-029 | 测试屏幕效果按钮 | 点击"💡 屏幕" | 顶部出现橙色跑马灯 + 全屏橙色爆闪 |
| BB-030 | 预警导出 | 点击"📤 导出" | 下载 JSON 文件，内容包含所有预警配置 |
| BB-031 | 预警导入 | 点击"📥 导入"，选择之前导出的 JSON | 预警列表增加，重复的被跳过 |
| BB-032 | 预警导入-无效文件 | 导入一个非 JSON 文件 | Toast 提示"导入失败：文件格式无效" |
| BB-033 | 预警间隔设置 | 设置间隔为 30 秒 | 添加后显示"⏱ 30秒"标签 |
| BB-034 | 预警间隔预设 | 下拉选择"5分钟"预设 | 输入框自动填入 5，单位变为 分钟 |
| BB-035 | 预警徽章-表头 | 触发预警后观察浏览器标签页 | 标签标题变为 🔴(N) Crypto Monitor |
| BB-036 | 预警徽章-铃铛图标 | 有活跃预警时观察铃铛按钮 | 显示红色数字徽章 |

### 2.3 强制预警模块

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-037 | 添加强制预警 | 选 BTC，跌幅 5%，窗口 30min，冷却 5min，点添加 | 预警出现在列表，监控面板显示进度条 |
| BB-038 | 删除强制预警 | 点击 🗑️ | 预警从列表消失 |
| BB-039 | 暂停/启用 | 点击 ⏸️ 暂停，再点 ▶️ 启用 | 状态切换，启用时重置冷却 |
| BB-040 | 强制预警触发 | 等待币种在窗口内跌幅超过阈值 | ①紧急语音播报（无视语音关闭）②紧急音 ③跑马灯(红色) ④爆闪(红色) ⑤浏览器通知 |
| BB-041 | 强制预警-语音无视开关 | 关闭语音开关，等待触发 | 强制预警仍会语音播报 |
| BB-042 | 测试爆闪按钮 | 点击"💡 爆闪" | 全屏红色爆闪 + 红色跑马灯 |
| BB-043 | 测试强制语音 | 点击"🗣️ 测试语音" | 播报紧急预警内容 + 紧急音效 |
| BB-044 | 实时跌幅监控 | 添加强制预警后打开面板 | 显示进度条，显示"当前跌幅 X% / 阈值 Y%" |
| BB-045 | 强制预警导入/导出 | 导出 → 导入验证 | 数据完整保留 |

### 2.4 声音与语音

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-046 | 声音开关-关闭 | 点击 🔊 按钮切换为 🔇 | 后续预警不再有声音 |
| BB-047 | 声音开关-开启 | 再次点击切换为 🔊 | 播放测试音，后续预警有声音 |
| BB-048 | 语音开关-开启 | 点击 🗄️ 切换为 🗣️ | 语音播报"语音播报已开启" |
| BB-049 | 语音开关-关闭 | 再次点击切换为 🔇 | 预警不再语音播报（强制预警除外） |
| BB-050 | 币种读法切换 | 点击 🀄 切换为 🔤 | Toast 提示"币种将逐字母拼读"，预警播报 BTC 变为 B-T-C |
| BB-051 | 币种读法-中文 | 再次点击切换回 🀄 | 播报"比特币"而非 B-T-C |
| BB-052 | 屏幕效果开关-关闭 | 点击 💡 切换为 ⬛ | 预警触发时不再有跑马灯/爆闪 |
| BB-053 | 屏幕效果开关-开启 | 再次点击 | 演示一次效果，后续预警恢复屏幕效果 |

### 2.5 通知渠道

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-054 | Server酱配置 | 打开通知设置，开启 Server酱，粘贴 SendKey，点测试 | 状态显示"✅ 发送成功"，微信收到测试消息 |
| BB-055 | Server酱-无效Key | 输入错误的 Key，点测试 | 状态显示"❌ 发送失败" |
| BB-056 | PushPlus配置 | 开启 PushPlus，粘贴 Token，点测试 | 微信收到测试消息 |
| BB-057 | Email配置 | 填入 Service ID / Template ID / Public Key / 邮箱，点测试 | 邮箱收到测试邮件 |
| BB-058 | 通知渠道持久化 | 配置好后刷新页面 | 配置仍然存在，开关状态保留 |
| BB-059 | iOS 通知权限 | 点击"开启通知权限"，允许 | 按钮变为"✅ 已授权"，可发送测试通知 |

### 2.6 持仓盈亏

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-060 | 添加持仓 | 打开持仓面板，选 BTC，买入价 60000，数量 0.5，点添加 | 持仓列表出现条目，显示成本 $30,000 |
| BB-061 | 盈亏计算 | 等价格更新后查看持仓面板 | 显示实时盈亏金额和百分比 |
| BB-062 | 盈亏-盈利 | 当前价 > 买入价 | 盈亏显示绿色 + 正值 |
| BB-063 | 盈亏-亏损 | 当前价 < 买入价 | 盈亏显示红色 + 负值 |
| BB-064 | 列表内嵌盈亏 | 查看行情列表中已持仓的币种 | 币种名旁显示盈亏百分比标签 |
| BB-065 | 删除持仓 | 点击持仓条目 ✕ | 持仓消失，列表中盈亏标签移除 |
| BB-066 | 总盈亏汇总 | 添加多个持仓后查看顶部汇总 | 显示总成本/总市值/总盈亏 |

### 2.7 市场数据

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-067 | 市场概况 | 首次加载后查看市场概况栏 | 显示总市值、BTC占比、恐惧贪婪指数、24h变化 |
| BB-068 | 热搜榜加载 | 等待页面加载 | 显示"🔥 实时热搜"/"💎 市值头部"/"📈 24h涨幅榜"三个分组 |
| BB-069 | 热搜刷新 | 点击热搜栏"刷新"按钮 | 按钮进入刷新状态，完成后显示新数据条数 |
| BB-070 | 恐惧贪婪颜色 | 观察恐惧贪婪指数 | 极恐=红色，恐惧=橙色，中性=默认，贪婪=绿色 |

### 2.8 新闻与解锁

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-071 | 新闻加载 | 点击 📰 打开新闻面板 | 显示新闻列表，含来源/标题/时间/相关币种标签 |
| BB-072 | 新闻筛选-自选 | 点击"自选币种"筛选 | 只显示与自选币种相关的新闻 |
| BB-073 | 新闻筛选-重要 | 点击"⚠️ 重要"筛选 | 只显示标记为重要的新闻 |
| BB-074 | 新闻刷新 | 点击"🔄 刷新" | 重新拉取数据，Toast 显示条数 |
| BB-075 | 新闻链接 | 点击某条新闻卡片 | 在新标签页打开原文链接 |
| BB-076 | 解锁事件加载 | 点击 🔓 打开解锁面板 | 显示解锁事件列表，含日期/数量/占比/进度条 |
| BB-077 | 解锁筛选-7天 | 点击"7天内"筛选 | 只显示7天内的解锁事件 |
| BB-078 | 解锁筛选-自选 | 点击"自选币种"筛选 | 只显示自选币种的解锁事件 |
| BB-079 | 解锁进度条 | 观察某解锁事件的进度条 | 颜色随已解锁百分比变化（绿→黄→红） |

### 2.9 AI 分析

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-080 | 市场全景分析 | 点击 🤖 → "市场全景分析" | 显示加载动画，1.5-2.5秒后出报告，含市场概况/BTC走势/恐惧贪婪解读/策略建议 |
| BB-081 | 自选币种诊断 | 点击"自选币种诊断" | 显示每个币种的 RSI/量能/趋势/综合评分 |
| BB-082 | 入手区间建议 | 点击"入手区间建议" | 显示支撑/阻力区间 + 分批建仓策略 |
| BB-083 | 风险评估 | 点击"风险评估" | 含持仓风险分析 + 各币种止损止盈建议 + 风控建议 |
| BB-084 | AI分析-无自选币种 | 删除所有币种后点"自选币种诊断" | 提示"请先添加自选币种" |

### 2.10 主题与外观

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-085 | 切换到浅色主题 | 点击 🌙 变为 ☀️ | 整体界面变白/浅色，所有元素适配 |
| BB-086 | 切换回深色主题 | 点击 ☀️ 变为 🌙 | 恢复深色背景 |
| BB-087 | 浅色主题下预警效果 | 切到浅色主题后触发预警 | 跑马灯/爆闪在浅色背景上依然可见（颜色加深） |
| BB-088 | 涨跌颜色反转 | 点击 🔄 变为 🔃 | 绿涨红跌 → 红涨绿跌，Sparkline 颜色同步变化 |
| BB-089 | 恢复默认颜色 | 再次点击 | 恢复绿涨红跌 |
| BB-090 | 主题持久化 | 切换主题后刷新页面 | 主题状态保留 |

### 2.11 数据管理

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-091 | 全量导出 | 点击 📤 | 下载 JSON 文件，含币种/预警/持仓/配置等全部数据 |
| BB-092 | 全量导入 | 点击 📥 选择之前导出的文件 | 数据恢复，Toast 提示导入成功，去重跳过已存在的币种 |
| BB-093 | 导入无效文件 | 导入非 JSON 格式文件 | Toast 提示"文件格式无效" |
| BB-094 | 导入去重 | 导入包含已有币种的文件 | 跳过重复，只新增，提示"新增 X 个，跳过 Y 个" |

### 2.12 快捷键

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-095 | `/` 聚焦搜索 | 按 `/` 键 | 搜索框获得焦点 |
| BB-096 | `A` 打开预警 | 按 `A` 键 | 价格预警弹窗打开 |
| BB-097 | `F` 打开强制预警 | 按 `F` 键 | 强制预警弹窗打开 |
| BB-098 | `P` 打开持仓 | 按 `P` 键 | 持仓弹窗打开 |
| BB-099 | `H` 打开帮助 | 按 `H` 键 | 快捷键帮助弹窗打开 |
| BB-100 | `S` 切换声音 | 按 `S` 键 | 声音开关切换 |
| BB-101 | `T` 切换主题 | 按 `T` 键 | 深色/浅色主题切换 |
| BB-102 | `Esc` 关闭弹窗 | 打开任意弹窗后按 Esc | 弹窗关闭 |
| BB-103 | 输入框中禁用快捷键 | 在搜索框中输入时按 `A` | 不触发预警弹窗，正常输入字符 |

### 2.13 响应式与兼容性

| TC-ID | 测试场景 | 操作步骤 | 预期结果 |
|-------|---------|---------|---------|
| BB-104 | 手机端布局 | 在 375px 宽度下访问 | 工具栏可滚动/换行，表格适配，字号缩小 |
| BB-105 | PC端布局 | 在 1920px 宽度下访问 | 最大宽度限制 1200px，居中显示，字号较大 |
| BB-106 | 手机端跑马灯 | 手机上触发预警 | 跑马灯横幅高度 38px，字号 0.82rem，不遮挡太多内容 |
| BB-107 | PC端跑马灯 | PC上触发预警 | 跑马灯横幅高度 52px，字号 1.2rem |
| BB-108 | 刘海屏适配 | 在有刘海的手机上触发跑马灯 | 跑马灯在刘海下方，不被遮挡 |
| BB-109 | 减弱动画 | 设置系统"减弱动态效果"后触发预警 | 跑马灯改为静态居中，爆闪减弱为3次慢闪 |
| BB-110 | 工具栏溢出 | 在窄屏上观察工具栏 | 按钮自动换行或可水平滚动 |

---

## 三、白盒测试用例（基于代码逻辑，覆盖内部路径）

### 3.1 数据获取与解析

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-001 | `fetchWithCors` 代理降级 | Mock 所有 CORS 代理返回 HTTP 500 | 抛出 "all proxies failed" 错误，isNetworkError=true |
| WB-002 | `fetchWithCors` 超时 | Mock 代理请求超时 6s | abort 触发，错误记录为 timeout，进入下一代理 |
| WB-003 | `fetchWithCors` 熔断器 | 连续失败 2 次同一代理 | proxyFailCounts 达到阈值，该代理进入 60s 冷却，后续请求跳过 |
| WB-004 | `fetchWithCors` 成功恢复 | 熔断后冷却到期 + 成功请求 | 熔断状态清除，代理恢复正常 |
| WB-005 | `cgThrottle` 节流 | 连续调用 3 次 cgFetch | 每次间隔 ≥ 1200ms（CoinGecko 限流保护） |
| WB-006 | `limitPool` 并发控制 | 提供 10 个任务，max=6 | 最多同时执行 6 个，全部完成后返回 10 个结果 |
| WB-007 | 交易所解析-Gate.io | 输入 `[{last:"50000",high_24h:"51000",low_24h:"49000",quote_volume_24h:"1000000",change_percentage:"2.5"}]` | 返回 `{p:50000,h:51000,l:49000,v:1000000,c:2.5}` |
| WB-008 | 交易所解析-Binance | 输入 `{lastPrice:"50000",highPrice:"51000",lowPrice:"49000",quoteVolume:"1000000",priceChangePercent:"2.5"}` | 返回正确的解析结果 |
| WB-009 | 交易所解析-无数据 | 输入 `{}` 或 null | 返回 null，不崩溃 |
| WB-010 | `fmtRaw` 格式化-大数 | `fmtRaw(88000)` | 返回 `"88,000.00"` |
| WB-011 | `fmtRaw` 格式化-小数 | `fmtRaw(0.00001234)` | 返回 `"0.00001234"`（8位小数） |
| WB-012 | `fmtRaw` 格式化-null | `fmtRaw(null)` | 返回 `"—"` |
| WB-013 | `fmtVol` 格式化-亿级 | `fmtVol(500000000)` | 返回 `"5.00亿 USDT"` |
| WB-014 | `fmtVol` 格式化-万级 | `fmtVol(150000)` | 返回 `"15.00万 USDT"` |
| WB-015 | `fmtVol` 格式化-零 | `fmtVol(0)` | 返回 `"—"` |

### 3.2 WebSocket 管理

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-016 | `isWsEligible` | 传入 BTC/ETH/SOL 和 XYZ123 | BTC/ETH/SOL 返回 true，XYZ123 返回 false |
| WB-017 | `wsGetBackoffDelay` | 连续调用 3 次（base=2000） | 第1次 ~2000ms+抖动，第2次 ~4000ms+抖动，第3次 ~8000ms+抖动（指数退避），上限 30s |
| WB-018 | Binance WS 消息解析 | Mock 收到 `{s:"BTCUSDT",c:"88000",h:"89000",l:"87000",q:"5000000",P:"1.5"}` | 调用 applyCoinData(sym='BTC', {p:88000,h:89000,l:87000,v:5000000,c:1.5}, 'Binance WS') |
| WB-019 | OKX WS 降级逻辑 | Mock Binance 数据 >15s 未更新时收到 OKX 数据 | OKX 数据被接受，wsProviderOf 更新为 'okx' |
| WB-020 | OKX WS 不覆盖新鲜数据 | Binance 2s 前刚更新，收到 OKX 数据 | OKX 数据被忽略，保持 Binance 数据 |
| WB-021 | `wsResync` 币种变化 | 添加一个 WS 可用币种后调用 | 检测到差异，触发 binanceWsConnect 重建连接 |
| WB-022 | `wsResync` 无变化 | 币种列表未变时调用 | 不触发重连 |
| WB-023 | WS 批量拆分 | 有 65 个 WS 可用币种 | Binance 分 3 批（30/30/5），OKX 分 2 批（50/15） |

### 3.3 价格预警逻辑

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-024 | `checkAlerts` 高于触发 | 设置 BTC above 85000，当前价 86000 | 触发预警，调用 showToast/speakAlert/playAlertSound/sendExternalNotification/sendBrowserNotification/vibrateDevice/triggerScreenAlert |
| WB-025 | `checkAlerts` 低于触发 | 设置 BTC below 80000，当前价 79000 | 触发预警 |
| WB-026 | `checkAlerts` 未触发 | 设置 BTC above 90000，当前价 85000 | 不触发，_everTriggered=false，lastNotified=0 |
| WB-027 | `checkAlerts` 冷却中 | 同一预警 3s 内连续触发（默认 5s 冷却） | 第二次不触发（在冷却期内） |
| WB-028 | `checkAlerts` 冷却到期 | 冷却到期后价格仍在阈值外 | 重新触发 |
| WB-029 | `checkAlerts` 价格回归重置 | 价格回到阈值内再出去 | _everTriggered 重置为 false，lastNotified 重置为 0，下次触发为"首次" |
| WB-030 | `checkAlerts` 首次 vs 后续 | 首次触发后 _everTriggered=true | 首次用 playUrgentSound，后续用 playAlertSound |
| WB-031 | `checkAlerts` 币种不匹配 | 有 BTC 预警，传入 ETH 数据 | 不触发 BTC 预警 |
| WB-032 | `ALERT_COOLDOWN_DEFAULT_MS` | 默认值检查 | 等于 5000ms |

### 3.4 强制预警逻辑

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-033 | `recordForceSnapshot` | 有 BTC 强制预警，传入价格 88000 | 记录快照 `{p:88000, t:Date.now()}` |
| WB-034 | `recordForceSnapshot` 节流 | 连续传入相同价格 | 只记录一次（跳过重复） |
| WB-035 | `recordForceSnapshot` 清理过期 | 传入超出 25h 的快照 | 过期快照被清除 |
| WB-036 | `recordForceSnapshot` 无预警币种 | 传入没有强制预警的币种 | 不记录快照 |
| WB-037 | `calcWindowDrop` 正常 | 窗口内价格从 90000 跌到 85000 | drop = (90000-85000)/90000*100 = 5.56% |
| WB-038 | `calcWindowDrop` 数据不足 | 只有 1 个快照 | 返回 `{drop:0, fromPrice:0, toPrice:0}` |
| WB-039 | `calcWindowDrop` 窗口外数据 | 最近快照都在窗口外 | 返回 drop=0 |
| WB-040 | `calcWindowDrop` 取窗口内最高价 | 窗口内有 85000→90000→87000→86000 | fromPrice=90000（窗口内峰值），toPrice=86000 |
| WB-041 | `checkForceAlerts` 触发 | 跌幅 5.5% > 阈值 5% | 触发：forceSpeakAlert/playUrgentSound/vibrateDevice/triggerScreenAlert/sendExternalNotification/sendBrowserNotification |
| WB-042 | `checkForceAlerts` 冷却中 | 触发后 1 分钟内再次检测（默认 5min 冷却） | 不触发 |
| WB-043 | `checkForceAlerts` 未达阈值 | 跌幅 3% < 阈值 5% | 不触发 |
| WB-044 | `checkForceAlerts` disabled | 预警 enabled=false | 跳过，不检测 |
| WB-045 | `forceSpeakAlert` 无视开关 | voiceEnabled=false 时调用 | 仍然播报（临时设为 true，播报后恢复） |
| WB-046 | `syncForceMonitors` 启动 | 有 enabled 的强制预警 | 启动 15s 定时器 |
| WB-047 | `syncForceMonitors` 停止 | 所有强制预警 disabled | 清除定时器 |

### 3.5 屏幕预警效果

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-048 | `triggerScreenAlert` 关闭状态 | screenAlertEnabled=false | 直接 return，不创建 DOM |
| WB-049 | `triggerScreenAlert` 冷却中 | 5s 内连续调用 | 第二次不触发跑马灯（冷却 10s），但爆闪不受冷却限制 |
| WB-050 | `_startMarquee` 创建 | 调用 _startMarquee('测试', false) | 创建 .alert-marquee-overlay DOM 元素，含 3 份重复文本 |
| WB-051 | `_startMarquee` 自动清理 | 等待持续时间（普通 5s/紧急 8s）后 | 添加 marquee-exit 类 → 350ms 后 remove 元素 |
| WB-052 | `_startMarquee` 旧元素清理 | 在跑马灯显示中再次触发 | 旧元素被 remove，新元素替换 |
| WB-053 | `_startMarquee` 移动端 | window.innerWidth=375 | scrollDuration 设为 5s，持续 4s |
| WB-054 | `_startMarquee` PC端 | window.innerWidth=1200 | scrollDuration 设为 8s，持续 5s |
| WB-055 | `_startMarquee` 紧急模式 | isUrgent=true | 持续时间更长（PC 8s / 手机 6s），文本含 🚨 |
| WB-056 | `_startStrobe` 创建 | 调用 _startStrobe(true) | 创建 strobe-red 类元素，2s 后自动 remove |
| WB-057 | `_startStrobe` 普通 | 调用 _startStrobe(false) | 创建 strobe-orange 类元素 |
| WB-058 | `toggleScreenAlert` 持久化 | 切换开关 | localStorage 'crypto_screen_alert' 更新为 '0' 或 '1' |

### 3.6 声音引擎

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-059 | `getAudioCtx` 懒初始化 | 首次调用 | 创建 AudioContext 实例 |
| WB-060 | `getAudioCtx` suspended 恢复 | AudioContext.state='suspended' | 自动调用 resume() |
| WB-061 | `getAudioCtx` closed 重建 | AudioContext.state='closed' | 重新创建新 AudioContext |
| WB-062 | `playAlertSound` 关闭 | soundEnabled=false | 直接 return，不播放 |
| WB-063 | `playAlertSound` 正常 | soundEnabled=true | 创建 3 个音符（A5→C#6→E6），连接 oscillator→filter→gain→destination |
| WB-064 | `playAlertSound` 降级 | AudioContext 不可用 | 调用 playFallbackBeep() |
| WB-065 | `playUrgentSound` | soundEnabled=true | 创建 2 组 × 3 音符（880/1175/1319Hz），共 6 个 oscillator |
| WB-066 | `playNotifySound` | soundEnabled=true | 创建 1 个滑音音符（C6→E6） |
| WB-067 | `playFallbackBeep` | AudioContext 不可用 | 使用 HTML5 Audio 播放内联 WAV beep |
| WB-068 | `unlockAudio` 首次调用 | audioUnlocked=false | 创建 AudioContext，播放静音 buffer，设置 audioUnlocked=true |
| WB-069 | `unlockAudio` 重复调用 | audioUnlocked=true | 直接 return |
| WB-070 | `recoverAudioContext` | AudioContext 被冻结 | 重建 AudioContext，重新注册 unlock 监听器 |

### 3.7 语音引擎

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-071 | `speakAlert` 语音关闭 | voiceEnabled=false | 直接 return |
| WB-072 | `speakAlert` 引擎未就绪 | speechReady=false | 调用 initSpeechEngine()，消息入 pendingSpeech 队列 |
| WB-073 | `speakAlert` 正常 | speechReady=true | 消息入 speechQueue，触发 processSpeechQueue |
| WB-074 | `speakAlert` 清理美元符号 | 输入含"$"的消息 | "$" 替换为 " 美元 " |
| WB-075 | `doSpeak` 正常播放 | 调用 doSpeak('测试') | 创建 SpeechSynthesisUtterance，设置中文语音，启动 watchdog |
| WB-076 | `doSpeak` watchdog 超时 | Mock onend 不触发 | watchdog 超时后 cancel + 回收，继续队列 |
| WB-077 | `doSpeak` speak 后未开始 | Mock speechSynthesis.speaking=false 500ms 后 | 尝试 resume，1s 后仍无响应则 cancel + 回收 |
| WB-078 | `processSpeechQueue` 空队列 | 队列为空时调用 | 直接 return |
| WB-079 | `processSpeechQueue` 正在播放 | speechPlaying=true 时调用 | 直接 return，不重复播放 |
| WB-080 | `processSpeechQueue` 引擎重置中 | speechEngineResetting=true | 直接 return |
| WB-081 | `recoverStalledSpeech` | 连续失败 < 3 次 | speechFailCount++，500ms 后 processSpeechQueue |
| WB-082 | `hardResetSpeechEngine` | 连续失败 ≥ 3 次 | cancel，重置 speechFailCount=0，1s 后重新 initSpeechEngine + 恢复队列 |
| WB-083 | `pickZhVoice` 中文匹配 | 语音列表含 zh-CN 语音 | 选择第一个 zh-CN 语音 |
| WB-084 | `pickZhVoice` 降级 | 无 zh-CN，有 zh-TW | 选择 zh-TW |
| WB-085 | `pickZhVoice` 无中文 | 语音列表只有英文 | zhVoice = null |
| WB-086 | `forceSpeakAlert` 无视开关 | voiceEnabled=false | 临时设为 true，播报后恢复 false |
| WB-087 | `getCoinSpeechName` 中文 | spellSymbols=false, sym='BTC' | 返回 '比特币' |
| WB-088 | `getCoinSpeechName` 拼读 | spellSymbols=true, sym='BTC' | 返回 'B T C' |
| WB-089 | `getCoinSpeechName` 未知币种 | sym='XYZ123' | 返回 'X Y Z 1 2 3'（逐字母拼读） |

### 3.8 保活系统

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-090 | WS 健康检查-数据停滞 | Mock WS 连接最后数据 >30s 前 | 主动关闭该连接，触发重连 |
| WB-091 | WS 健康检查-全部断开 | 所有 WS 连接状态非 OPEN | 触发 binanceWsConnect() 全量重连 |
| WB-092 | WS 健康检查-正常 | 所有连接正常，数据 <30s | 仅更新 UI 显示连接状态 |
| WB-093 | 计时漂移检测 | Mock setInterval 实际间隔 >15s | 调用 recoverFromSleep('计时漂移检测') |
| WB-094 | `recoverFromSleep` 完整恢复 | 距上次前台 >30s | ①重建 timer ②fetchPrices ③WS重连 ④AudioContext恢复 ⑤语音恢复 ⑥WakeLock ⑦市场数据刷新 |
| WB-095 | `recoverFromSleep` 轻量恢复 | light=true | 只 fetchPrices + updateWsStatus，不做其他恢复 |
| WB-096 | `recoverFromSleep` 冷却 | 5s 内连续调用完整恢复 | 第二次跳过（RECOVER_COOLDOWN=5s） |
| WB-097 | visibilitychange 短暂离开 | 离开 <10s 后返回 | 不做任何恢复 |
| WB-098 | visibilitychange 中等离开 | 离开 15s 后返回 | 轻量恢复（只拉价格） |
| WB-099 | visibilitychange 长时间离开 | 离开 60s 后返回 | 完整恢复 |
| WB-100 | Edge 专用事件 | Mock Edg/ UA + freeze/resume 事件 | resume 触发轻量恢复 |

### 3.9 存储与持久化

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-101 | `safeSetItem` 正常 | 正常写入 | localStorage.setItem 成功 |
| WB-102 | `safeSetItem` 配额溢出 | Mock QuotaExceededError | 调用 prunePriceHistory(100) 清理后重试 |
| WB-103 | `safeSetItem` 清理后仍溢出 | Mock 两次都抛 QuotaExceededError | 输出警告，不崩溃 |
| WB-104 | `prunePriceHistory` | priceHistory['BTC'] 有 300 条 | 清理后保留最后 100 条 |
| WB-105 | `loadCoins` 去重 | localStorage 含重复 BTC | 返回去重后的数组（只保留第一个） |
| WB-106 | `loadCoins` 默认值 | localStorage 无数据 | 返回 DEFAULT_COINS (BTC/ETH/WLFI) |
| WB-107 | `loadForceSnapshots` 清理过期 | 含 30h 前的快照 | 只保留 25h 内的快照 |
| WB-108 | `saveForceSnapshotsThrottled` 节流 | 60s 内连续调用 5 次 | 只实际写入 1 次 |

### 3.10 渲染与 DOM

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-109 | `render` 增量更新 | _lastRenderKey 未变时调用 | 跳过，不重建 DOM |
| WB-110 | `render` 强制更新 | render(true) | 无论 key 是否变化都重建 DOM |
| WB-111 | `render` 空列表 | coins=[] | rows 清空，显示 empty 状态 |
| WB-112 | `applyCoinData` DOM 更新 | 传入 BTC 数据 | 更新 p-BTC/h-BTC/l-BTC/v-BTC/c-BTC/sp-BTC 各元素 |
| WB-113 | `applyCoinData` 闪动类 | 前价 80000 → 现价 81000 | p-BTC 元素添加 flash-up 类 |
| WB-114 | `applyCoinData` 闪动-下跌 | 前价 81000 → 现价 80000 | p-BTC 元素添加 flash-down 类 |
| WB-115 | `applyCoinData` 持仓标签 | BTC 有持仓记录 | coin-name 内更新 getPnlHtml |
| WB-116 | `sparklineSvg` 正常 | priceHistory 有 20 个价格点 | 返回 SVG polyline，颜色与首尾价格涨跌一致 |
| WB-117 | `sparklineSvg` 数据不足 | priceHistory < 2 条 | 返回空字符串 |
| WB-118 | `escapeHtml` XSS 防护 | 输入 `<script>alert(1)</script>` | 返回转义后的安全文本 |
| WB-119 | `escapeAttr` 注入防护 | 输入 `"><img src=x onerror=alert(1)>` | 返回转义后的安全属性值 |

### 3.11 排序逻辑

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-120 | `sortBy` 切换键 | 从 price 切换到 change | sortState.key='change', 默认 desc |
| WB-121 | `sortBy` 同键反转 | 再次 sortBy('change') | sortState.asc 取反 |
| WB-122 | `getSortVal` price | ph 含 {p:88000} | 返回 88000 |
| WB-123 | `getSortVal` 无数据 | 无 priceHistory | 返回 -Infinity |
| WB-124 | `getSortedCoins` 升序 | sortState={key:'price',asc:true} | 价格最低的排在最前 |
| WB-125 | `getSortedCoins` 降序 | sortState={key:'change',asc:false} | 涨幅最大的排在最前 |

### 3.12 预警导入导出

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-126 | `exportAlerts` 空列表 | alerts=[] | Toast 提示"暂无预警数据可导出"，不下载文件 |
| WB-127 | `exportAlerts` 有数据 | alerts=[{symbol:'BTC',dir:'above',price:90000}] | 下载 JSON，含 version/type/alerts/exportedAt |
| WB-128 | `importAlerts` 格式兼容 | 导入含 type:'alerts' 的文件 | 正确解析 |
| WB-129 | `importAlerts` 全局文件兼容 | 导入全局导出文件（含 alerts 字段） | 兼容解析 |
| WB-130 | `importAlerts` 去重 | 导入含已存在预警的文件 | 跳过重复，只新增 |
| WB-131 | `importAlerts` 无效格式 | 导入不含 alerts 的 JSON | Toast 提示"未找到预警数据" |

### 3.13 持仓计算

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-132 | `getPnlHtml` 无持仓 | 该币种无持仓 | 返回 `"—"` |
| WB-133 | `getPnlHtml` 盈利 | 买入价 60000, 当前价 66000 | 显示 `+10.0%`，class=profit |
| WB-134 | `getPnlHtml` 亏损 | 买入价 66000, 当前价 60000 | 显示 `-9.1%`，class=loss |
| WB-135 | `getPnlHtml` 多笔持仓 | 两笔 BTC 持仓（不同买入价） | 合并计算总成本和总市值 |

### 3.14 币种搜索

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-136 | `onSearchInput` 空值 | 输入为空 | 下拉关闭 |
| WB-137 | `onSearchInput` 本地匹配 | 输入 'BT'，coins 含 BTC | 立即显示 BTC（不等 API） |
| WB-138 | `onSearchInput` API 搜索 | 输入 'sol'，350ms 后 | CoinGecko 返回结果，与本地结果合并 |
| WB-139 | `onSearchKey` Enter 选中 | searchIdx=2，按 Enter | 调用 selectSearchResult(2) |
| WB-140 | `onSearchKey` Enter 无选中 | searchIdx=-1，有结果 | 默认选中第一个 |
| WB-141 | `onSearchKey` ArrowDown | searchIdx=0，按 ↓ | searchIdx=1，高亮切换 |
| WB-142 | `onSearchKey` ArrowUp 边界 | searchIdx=0，按 ↑ | searchIdx 保持 0（不越界） |

### 3.15 通知发送

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-143 | `sendExternalNotification` 全渠道 | 所有渠道已启用并配置 | 并行发送 Server酱/PushPlus/Email/iOS Push |
| WB-144 | `sendExternalNotification` 无渠道 | 所有渠道 disabled | 不发送任何通知，不报错 |
| WB-145 | `sendServerChan` CORS 降级 | 直连失败 | 自动尝试 corsproxy.io 代理 |
| WB-146 | `sendBrowserNotification` 权限拒绝 | Notification.permission='denied' | 直接 return，不抛异常 |
| WB-147 | `sendBrowserNotification` SW 优先 | ServiceWorker 已注册 | 通过 reg.showNotification 发送 |

### 3.16 Force Alert 导入导出

| TC-ID | 测试目标 | 测试方法 | 预期结果 |
|-------|---------|---------|---------|
| WB-148 | `exportForceAlerts` 空列表 | forceAlerts=[] | Toast 提示"暂无强制预警可导出" |
| WB-149 | `importForceAlerts` 去重 | 导入含已有预警的文件 | 跳过重复项 |
| WB-150 | `importForceAlerts` 无效 | 导入无 forceAlerts 字段的 JSON | Toast 提示"格式无效" |

---

## 四、测试覆盖统计

| 类别 | 黑盒用例数 | 白盒用例数 | 小计 |
|------|-----------|-----------|------|
| 行情监控 | 20 | 15 | 35 |
| 价格预警 | 16 | 9 | 25 |
| 强制预警 | 9 | 15 | 24 |
| 声音与语音 | 8 | 31 | 39 |
| 通知渠道 | 5 | 5 | 10 |
| 持仓盈亏 | 7 | 4 | 11 |
| 市场数据 | 4 | — | 4 |
| 新闻与解锁 | 9 | — | 9 |
| AI 分析 | 5 | — | 5 |
| 主题外观 | 6 | — | 6 |
| 数据管理 | 4 | 6 | 10 |
| 快捷键 | 9 | — | 9 |
| 响应式兼容 | 7 | — | 7 |
| WebSocket | — | 8 | 8 |
| 保活系统 | — | 11 | 11 |
| 存储持久化 | — | 8 | 8 |
| 渲染DOM | — | 11 | 11 |
| 排序逻辑 | — | 6 | 6 |
| 币种搜索 | — | 7 | 7 |
| **合计** | **118** | **136** | **254** |
